'use strict';

const urls = {

    NAME: 'urls',
    COMMON_NAME: 'urls',
    FILTERS: [],
    COLUMNS: [
        'id',
        'url',
        'status',
        'createdAt'
    ],
    UPDATABLE_COLUMNS: [
        'url',
        'status'
    ],
    RESTRICTED_COLUMNS: {},
    PATTERN_MATCH_COLUMNS: []
};

module.exports = urls;
